<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appmediclass1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activitycitas_history" android:parentActivityName=".Activity_citas" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Activity_citas" android:parentActivityName=".Activity_ingresar" />
        <activity android:name=".Activity_ingresar" android:parentActivityName=".MainActivity" />
        <activity android:name=".Activity_registar" android:parentActivityName=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>